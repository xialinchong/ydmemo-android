<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/FrameLayout1"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@null" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:paddingTop="5dp"
        android:orientation="vertical" >

        <com.yidianhulian.ydmemo.view.CircleImageView
            android:id="@+id/memo_user_avatar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/avatar" />

        <TextView
            android:id="@+id/memo_user_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="middle"
            android:maxLines="1"
            android:text="@string/placeholder"
            android:textSize="12sp"
            android:textColor="@color/datecolor" />
    </LinearLayout>

    <CheckBox
        android:id="@+id/memo_user_checked"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:button="@null"
        android:background="@drawable/checkbox_selector"
        android:layout_gravity="bottom|right" />
    
    <ImageView
        android:id="@+id/memo_user_pending"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginTop="3dp"
        android:src="@drawable/busy"
        android:layout_gravity="top|right" />
    <Button
        android:id="@+id/avatar_btn_press"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@drawable/mask_btn_selector" />

</FrameLayout>